# secrets -- NEVER commit (provisioned at deploy, kept on-node)
seeds.txt
*.token
publish.token
key.pem
tls/
# build
target/
result
result-*
seeds.env

# build artifacts -- rebuilt from source, NOT committed (public repo)
*.wasm
/ha-3node/store
/store-publishd/store-publishd
